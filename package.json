{
  "name": "navirice-rendering-server",
  "version": "1.0.0",
  "description": "Rendering Service ===",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "cd ../common && npm run build && cd ../rendering && cd build && rm -rf * && cmake .. && make",
    "server": "./build/bin/server",
    "client": "./build/bin/client"
  },
  "author": "",
  "license": "ISC"
}
